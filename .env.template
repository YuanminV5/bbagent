# bbagent uses Claude Code SDK — no API key needed.
# Authentication is handled by your existing Claude Code login.
# Run `claude auth login` if you haven't logged in yet.
#
# Optional: override which Claude model is used (default: whatever Claude Code uses)
# ANTHROPIC_MODEL=claude-opus-4-7
